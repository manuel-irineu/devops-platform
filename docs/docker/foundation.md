# Docker Foundation

## Goal

Validate the Docker installation and understand the basic lifecycle of images and containers.

## Commands executed

```bash
docker image ls
docker run -it --rm debian:13 bash
