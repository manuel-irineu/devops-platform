---
- name: Bootstrap DevOps lab VM
  hosts: local
  become: true
  gather_facts: true

  vars:
    project_root: "{{ playbook_dir }}/../../.."

  roles:
    - common
    - docker
    - compose_stacks
